<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.geekhour.loki.mapper.BudgetExecutionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="net.geekhour.loki.entity.BudgetExecution">
        <result column="id" property="id" />
        <result column="budget_id" property="budgetId" />
        <result column="execution_amount" property="executionAmount" />
        <result column="execution_reason" property="executionReason" />
        <result column="execution_date" property="executionDate" />
        <result column="version" property="version" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="deleted" property="deleted" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,
        budget_id, execution_amount, execution_reason, execution_date, version, create_time, update_time, deleted
    </sql>

</mapper>
